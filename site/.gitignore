# build output
dist/
.astro/

# deps
node_modules/

# generated data cache (regenerated by scripts/build-data.mjs on pre(dev|build))
# NOTE: src/data/images.json IS committed as a seed so a fresh clone renders
# without a data run; it is overwritten by every build. Do not ignore it.

# local report fixtures used for full-data testing (see README)
reports/

# env / logs
.env
.env.*
npm-debug.log*
