# Melange build configuration for minimal Gitea
# Builds Gitea from source (Go + Node.js frontend) with SQLite support
# CGO_ENABLED=1 required for SQLite — produces dynamically linked binary

package:
  name: gitea-minimal
  version: 1.26.3
  epoch: 0
  description: "Minimal Gitea git hosting server built from source"
  copyright:
    - license: MIT

vars:
  # SHA256 checksum of source tarball
  sha256: ae04ccc547ac3a56e576a934ee37b1864953b8ca13658556dd8f6072ed96a0d5

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - curl
      - build-base
      - go
      - git
      # Frontend build dependencies
      - nodejs-22
      - pnpm
      # SQLite support (CGO_ENABLED=1)
      - sqlite-dev

pipeline:
  # Download and verify Gitea source
  - runs: |
      mkdir -p /home/build
      curl -fsSL --retry 5 --retry-all-errors "https://github.com/go-gitea/gitea/archive/refs/tags/v${{package.version}}.tar.gz" \
        -o /home/build/gitea.tar.gz

      echo "${{vars.sha256}}  /home/build/gitea.tar.gz" | sha256sum -c -

      cd /home/build
      tar xzf gitea.tar.gz
      rm gitea.tar.gz


  # patch-go-deps: auto-generated — do not edit manually
  - runs: |
      export GOWORK=off
      _retry() { local i; for i in 5 15 30; do "$@" && return 0; echo "retry: $* failed, sleeping ${i}s"; sleep $i; done; "$@"; }
      for root in /home/build/gitea-${{package.version}}; do
        [ -f "$root/go.mod" ] || continue
        ( cd "$root" \
          && _retry go get github.com/Azure/go-ntlmssp@v0.1.1 github.com/go-git/go-git/v5@v5.19.1 golang.org/x/image@v0.41.0 golang.org/x/crypto@v0.53.0 golang.org/x/net@v0.56.0 golang.org/x/sys@v0.46.0 golang.org/x/text@v0.38.0 golang.org/x/term@v0.44.0 golang.org/x/time@v0.15.0 golang.org/x/sync@v0.21.0 golang.org/x/tools@v0.46.0 \
          && go mod tidy -e \
          && { [ ! -d vendor ] || go mod vendor; } )
      done
  # end-patch-go-deps
  # Build frontend assets and backend binary via Makefile
  # make frontend: pnpm install + vite build
  # make backend:  go generate (creates bindata.dat embeds) + go build
  - runs: |
      cd /home/build/gitea-${{package.version}}
      GOTOOLCHAIN=local \
      TAGS="bindata sqlite sqlite_unlock_notify" \
      CGO_ENABLED=1 \
      EXTRA_GOFLAGS="-trimpath" \
      GITEA_VERSION=${{package.version}} \
      make build

  # Install to destdir
  - runs: |
      mkdir -p "${{targets.destdir}}/usr/bin"
      cp /home/build/gitea-${{package.version}}/gitea "${{targets.destdir}}/usr/bin/"

  # Verify the build
  - runs: |
      echo "Verifying Gitea build..."
      "${{targets.destdir}}/usr/bin/gitea" --version
      echo "Checking binary size..."
      ls -lh "${{targets.destdir}}/usr/bin/gitea"

update:
  enabled: true
  github:
    identifier: go-gitea/gitea
    use-tag: true
    tag-filter: "v1."
