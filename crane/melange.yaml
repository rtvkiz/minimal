# Melange build configuration for minimal crane
# Pure Go from source. Single static `crane` binary. License: Apache-2.0.

package:
  name: crane-minimal
  version: 0.21.9
  epoch: 0
  description: "Minimal crane OCI registry tool built from source"
  copyright:
    - license: Apache-2.0

vars:
  # SHA256 of crane source tarball - updated by update-versions.yml workflow
  sha256: 6d8bce869afcc485b518cc0d59ea0ffe1090026db965806bc3be8793182528cc

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - curl
      - build-base
      - go
      - git

pipeline:
  - runs: |
      mkdir -p /home/build/crane-${{package.version}}
      curl -fsSL --retry 5 --retry-all-errors \
        "https://github.com/google/go-containerregistry/archive/refs/tags/v${{package.version}}.tar.gz" \
        -o /home/build/crane.tar.gz
      echo "${{vars.sha256}}  /home/build/crane.tar.gz" | sha256sum -c -
      tar xzf /home/build/crane.tar.gz -C /home/build/crane-${{package.version}} --strip-components=1
      rm /home/build/crane.tar.gz

  # patch-go-deps: auto-generated — do not edit manually
  - runs: |
      export GOWORK=off
      _retry() { local i; for i in 5 15 30; do "$@" && return 0; echo "retry: $* failed, sleeping ${i}s"; sleep $i; done; "$@"; }
      _pgd_reqs() { awk '$1=="require" && $2=="(" {b=1;next} b && $1==")" {b=0;next} b && index($1,"/")>0 && substr($2,1,1)=="v" {print $1,$2;next} $1=="require" && NF>=3 && index($2,"/")>0 && substr($3,1,1)=="v" {print $2,$3}' "$1"; }
      for root in /home/build/crane-${{package.version}}; do
        [ -f "$root/go.mod" ] || continue
        ( set -e
          cd "$root"
          cp go.mod .pgd-orig.mod
          _retry go get github.com/klauspost/compress@v1.18.7
          go mod tidy -e
          # go mod tidy can downgrade a source-pinned require below what the
          # source shipped (e.g. a lockstep family like openbao's
          # go-kms-wrapping transit v2.8.0 -> v2.1.0, which then fails to build
          # against its core sibling still at v2.8.0). Re-raise any require tidy
          # dropped below its pre-tidy version. Corrects only upward, so CVE
          # bumps (>= source) are never undone.
          _pgd_reqs .pgd-orig.mod > .pgd-o.txt
          _pgd_reqs go.mod > .pgd-n.txt
          awk 'NR==FNR{o[$1]=$2;next} ($1 in o) && o[$1]!=$2 {print $1,o[$1],$2}' .pgd-o.txt .pgd-n.txt | while read -r p ov cv; do
            hi=$( { echo "$ov"; echo "$cv"; } | sort -V | tail -1 )
            if [ "$hi" = "$ov" ]; then
              echo "pgd-guard: restoring $p: $cv -> $ov (tidy downgraded below source pin)"
              _retry go get "$p@$ov"
            fi
          done
          rm -f .pgd-orig.mod .pgd-o.txt .pgd-n.txt
          [ ! -d vendor ] || go mod vendor
        )
      done
  # end-patch-go-deps
  - runs: |
      cd /home/build/crane-${{package.version}}
      GOTOOLCHAIN=local CGO_ENABLED=0 go build \
        -trimpath \
        -ldflags="-s -w -buildid= \
          -X github.com/google/go-containerregistry/cmd/crane/cmd.Version=${{package.version}}" \
        -o /home/build/crane-bin \
        ./cmd/crane

  - runs: |
      mkdir -p "${{targets.destdir}}/usr/bin"
      cp /home/build/crane-bin "${{targets.destdir}}/usr/bin/crane"
      chmod 0755 "${{targets.destdir}}/usr/bin/crane"

  - runs: |
      echo "Verifying crane binary..."
      "${{targets.destdir}}/usr/bin/crane" version
